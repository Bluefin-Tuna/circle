name: CircleRegression
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy==1.22.4
  - scikit-image
  - matplotlib
  - ipykernel
  - tqdm
  - torchvision
  - pandas
  - pytorch-cuda=11.8
  - torchaudio
  - cudatoolkit
  - pytorch[build=*cuda*]
prefix: /home/tanush/Miniconda/envs/CircleRegression
